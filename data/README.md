# Datasets

- `astram_events.csv` — PS2 incident data, trains the forecaster.
- `police_violations.csv` — PS1 parking data (~105MB), powers the hotspot map.

`police_violations.csv` is too large for normal git. Either use Git LFS or keep
it out of the repo and place it here locally on each machine.
