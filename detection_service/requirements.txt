fastapi
uvicorn[standard]
python-multipart
pillow

# Dev B adds the LocateAnything stack on the 3060 box:
# torch
# transformers
# accelerate

# YOLO baseline (DETECTOR=yolo) — fast, local, closed-vocabulary:
# ultralytics
# omegaconf      # required by the bundled license_plate_detector.pt
# easyocr        # license-plate OCR for YoloDetector
